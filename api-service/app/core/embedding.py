"""Embedding persistence constants (Phase 2 placeholder — no provider integration)."""

# Persistence-only dimension for vector columns and validation.
# Not tied to a live embedding model; replace when a real provider is wired.
EMBEDDING_DIMENSION = 1536

# Placeholder model name for local persistence and tests only.
DEFAULT_EMBEDDING_MODEL = "text-embedding-3-small"

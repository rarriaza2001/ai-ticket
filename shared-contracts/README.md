# shared-contracts

Cross-service **Pydantic schemas** for health/readiness and Phase 3 AI enums/DTOs (`SuggestionStatus`, `AiTicketEventType`, `AiPipelineObservability`). No SQLAlchemy, repositories, prompts, or provider SDKs.

Install as an editable path dependency from `api-service` or `ai-worker`.

# shared-contracts

Cross-service **Pydantic schemas** for Phase 1 foundation (health/readiness). No ticket, queue, or AI types here yet.

Install as an editable path dependency from `api-service` or `ai-worker`.
